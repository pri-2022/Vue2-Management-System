import Vue from 'vue'
import Vuex from 'vuex'
import HomeModule from '@/store/HomeModule'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    HomeModule,
  }
})
